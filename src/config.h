#pragma once
constexpr float METERS_PER_UNIT = 1.0f;     // 1 unit == 1 meter
constexpr float UNITS_PER_METER = 1.0f;
constexpr float MAP_SIDE_M = 105500.0f;
constexpr float MAP_HALF_M = MAP_SIDE_M * 0.5f;
